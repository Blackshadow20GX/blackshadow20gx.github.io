version: 0.2
phases:
  install:
    commands:
      - "npm install gulp -g"
      - "npm install"
  build:
    commands:
      - "gulp deploy-qa"
artifacts:
  base-directory: "dist"
  files:
    - "*"
